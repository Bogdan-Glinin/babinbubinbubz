import { Button, Layout } from "antd";

const Dashboard = () => {
  return <Layout>
    <Button href="/login">
        Перейти в личный кабинет
    </Button>
  </Layout>;
};

export default Dashboard;
